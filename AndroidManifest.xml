<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ru.piter.fm"
          android:versionCode="4"
          android:versionName="1.4">

    <uses-sdk android:minSdkVersion="3" android:targetSdkVersion="4"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    
    <application android:icon="@drawable/logo" android:label="PiterFM" android:name="ServiceHolder"
                 android:theme="@android:style/Theme.NoTitleBar">
        <activity android:name=".activities.MainActivity" android:launchMode="singleInstance"
                  android:configChanges="keyboardHidden|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.RadioActivity">

        </activity>
        <service android:name=".player.PlayerService"/>
        <receiver android:name=".receiver.EventsReceiver">
            <intent-filter>
                <action android:name="android.intent.action.CALL"/>
                <action android:name="ru.piter.fm.action.ERROR"/>
            </intent-filter>
        </receiver>
    </application>



</manifest>
